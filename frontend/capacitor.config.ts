import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartchili.harvest',
  appName: 'Smart Chili Harvest',
  webDir: 'dist'
};

export default config;
